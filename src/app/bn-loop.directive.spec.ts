import { BnLoopDirective } from './bn-loop.directive';

describe('BnLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLoopDirective();
    expect(directive).toBeTruthy();
  });
});
